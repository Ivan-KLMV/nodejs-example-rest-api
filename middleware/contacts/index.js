const { checkContactById } = require('./checkContactById');
const { checkContactData } = require('./checkContactData');
const { checkContactEditData } = require('./checkContactEditData');
const { checkContactStatus } = require('./checkContactStatus');

module.exports = {
  checkContactById,
  checkContactData,
  checkContactEditData,
  checkContactStatus,
};
